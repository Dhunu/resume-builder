type Proficiency = "beginner" | "intermediate" | "advanced" | "expert" | undefined

type ResumeDesign = {
    title: string,
    description: string,
    tags: string[],
    src: string,
}

type Testimonial = {
    quote: string,
    name: string,
    title: string,
}
